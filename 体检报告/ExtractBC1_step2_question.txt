任务：校对、补全并标记健康报告JSON

你是一个细致的数据验证专员。在我提供的附件中，有三个文件：

原始PDF报告： 这是所有信息的“唯一真相” (Source of Truth)。

JSON Schema： 这是数据结构的“规则手册” (Rulebook)。

已提取的JSON草稿： 这是需要你校对和修正的“草稿” (Draft)。

核心指令
你的目标是生成一个唯一、完整、准确且已标记的JSON文件。请严格遵循以下步骤：

以Schema为纲，补全所有结构：

JSON Schema (规则手册) 在 labResults.definitions 中定义了所有必需的 TestCategory （如 "尿常规", "血常规", "凝血常规" 等）以及每个类别下的所有 TestItem （如 "尿比重", "白细胞计数_血" 等）。

你的最终输出必须包含 Schema 中定义的每一个 TestCategory 和每一个 TestItem，不可缺少。

以PDF为据，核对和填充数据：

纠错： 仔细检查 JSON草稿 (草稿) 中的每一项数据。将其与 原始PDF报告 (真相) 进行比对。如果草稿中的值（value）或异常状态（isAbnormal）与PDF不符，请立即修正。

补缺 (最重要)： 针对你在步骤1中识别出的、Schema中存在但草稿中可能缺失的 TestItem（例如 "凝血常规" 或 "尿生化" 中的项目）： A. 你必须主动在 原始PDF报告 中搜索该项目的值。 B. 如果在PDF中能找到对应的值，请将其准确填充到JSON中。 C. 如果在PDF中确实找不到该项目（例如，这份报告就没有做 "凝血常规" 检查），你仍然必须在JSON中包含该 TestItem，但应将其 value 字段设置为 null。

3. 添加更新标记（新步骤）： * 在完成上述所有校对和补全后，你必须根据是否进行了修改，在最终的JSON文件中添加一个状态标记。 * 判定规则： 对比你生成的“最终文件”和原始的“JSON草稿”。 * 如果进行了修改： 如果你对“JSON草稿”进行了任何修正（如修改 value 或 isAbnormal）或补缺（如添加了 Schema 中定义但草稿中缺失的 TestItem，即使其值为 null），则视为有更新。 * 在这种情况下，请在“目标文件”的顶层（与 headerInfo 同级）添加键值对："Updated": "Y"。 * 如果未进行修改： 如果“JSON草稿”在所有校验后被证实 100% 准确且合规（无需任何修改或添加），则视为无更新。 * 在这种情况下，请添加键值对："Updated": "N"。

4. 输出： * 请不要提供任何分析或评论。 * 仅输出包含了 Updated 标记的、 最终校对、修正并补全后的完整JSON代码块。